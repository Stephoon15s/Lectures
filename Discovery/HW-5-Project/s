Compile: Start Name Server, Start List Server, Client doesn't seem necessary.

The third part of the assignemnt is meant to simulate a slower Server
and show that it will dies if the heartbeat is too fast.

Task List:
- in Main List-Server-Binary: Make new thread and have it run rgister with name server. Don't detach.
    - In this this thread, after finishing the register stuff, having it enter a new loop that will send out a heartbeat.
    Modify Register to do this. Thread sends a heartbeat request.

- Build Hearbeat Request is in Name Server Handler.
./list_server_binary 127.0.0.1 9091


6/18/2026:

I've implemented the changes to list-server-binary. I make a new thread to run register with name server. I enter an infinite loop that will pause 5 seconds before sending a heartbeat request. I know it is sent because it doesn't return.

Added the last Heart Beat Var
added the time update to when I make a service providwer object.